import "package:flutter/material.dart";

/// A stepper widget that displays template credential cards
/// that the user needs to obtain during a disclosure session.
class DisclosureTemplateStepper extends StatelessWidget {
  const DisclosureTemplateStepper({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement
    return const SizedBox.shrink();
  }
}
