import "package:flutter/material.dart";

/// Screen shown when the user needs to obtain multiple credentials
/// from different sources for a single disclosure choice.
class DisclosurePermissionObtainCredentialsScreen extends StatelessWidget {
  const DisclosurePermissionObtainCredentialsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement
    return const SizedBox.shrink();
  }
}
